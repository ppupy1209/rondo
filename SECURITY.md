# 보안 정책

## 지원 버전

최신 공개 릴리스만 보안 수정 대상입니다. 버전은 `rondo --version`으로 확인할 수 있습니다.

## 보안 경계

Rondo는 로컬 보조 도구이며 중앙 서버, Rondo 계정, 자체 모델 API를 사용하지 않습니다. 다음 원칙을 적용합니다.

- 프로젝트 상태는 링크·정션이 아닌 프로젝트 루트의 `.rondo/`에만 저장합니다.
- 상태 변경은 파일 잠금과 원자적 교체를 사용합니다.
- 컨텍스트는 32 KiB, 메시지는 1 MiB로 제한하고 전체 공급자 대화나 내부 추론을 읽지 않습니다.
- 일반적인 토큰 패턴은 기록 전에 가리지만 `.rondo/`를 비밀 저장소로 취급하지 않습니다.
- 인계할 AI가 신뢰·승인·선택 화면에 있으면 키 입력을 보내지 않습니다.
- 설치 릴리스는 SHA-256 목록으로 확인하고, Zellij은 고정 버전 자산의 내장 digest를 확인합니다. SHA-256은 전송 오류와 자산 불일치를 탐지하지만 코드 서명을 대신하지 않습니다.
- 0.14.x 전역 설정 정리는 정확히 알려진 Rondo 명령만 제거하며, 변경 전 백업을 남깁니다.

Rondo가 방어한다고 주장하지 않는 범위:

- 같은 운영체제 사용자 권한을 이미 획득한 악성 프로세스
- Codex, Claude, Gemini, Zellij, GitHub 또는 운영체제 자체의 취약점과 계정 침해
- 사용자가 직접 승인한 악성 저장소, 명령, 확장, MCP 서버
- 공급자 CLI가 자체 정책에 따라 저장하거나 전송하는 데이터

## 취약점 제보

토큰, 실제 공격 코드, 회사·고객 정보는 공개 issue에 올리지 마세요. GitHub 저장소의 **Security > Report a vulnerability**에서 비공개 권고 초안을 만들고 다음을 포함해 주세요.

- 영향을 받는 Rondo 버전과 운영체제
- 최소 재현 절차, 예상 결과와 실제 결과
- 데이터 노출 또는 권한 확대 범위
- 민감정보를 제거한 테스트 사례

재현 여부를 확인한 뒤 수정 릴리스와 공개 시점을 조율합니다.
